/**
 * CJK-aware text measurement utilities.
 * Re-exported from @agentdeck/shared for backward compatibility.
 */
export { isWide, charPx, measureTextWidth, sliceByPx, wrapTextByWidth } from '@agentdeck/shared';
